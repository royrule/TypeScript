import { Human } from '../human-list/human.model';

export class Cart {
    // private static instance: Cart;
    // private arrayC: Array<Human>;

    // constructor() {}

    // public static get Instance()
    // {
    // //    return this.instance || (this.instance = undefined) || (this.instance = null);
    // //    var Cart instance = new Cart();
    // }

}
